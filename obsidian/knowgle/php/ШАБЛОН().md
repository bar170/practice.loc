
```php
functionCon($x, $y)
```
Принимает аргументом строку `$x` и строку `$y`. Может принимать аргументы `$someArgument` и `$anotherArgument`. Возвращает конкатенацию строк `$x` и `$y`.

Пример: 
```php
$result = functionCon("Hello ", "World!"); 
# $result == "Hello World!"
```

#php #string