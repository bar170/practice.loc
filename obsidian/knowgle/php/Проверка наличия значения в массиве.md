# Проверка наличия значения в массиве

Поиск значения в массиве с возвратом булева результата.

## Задача с CodeWars

**You only need one - Beginner**

> You will be given an array `a` and a value `x`. All you need to do is check whether the provided array contains the value.  
> `a` can contain numbers or strings. `x` can be either.  
> Return `true` if the array contains the value, `false` if not.

## Пример

```php
$array = [1, 2, 3, 4, 5];
solution($array, 3); // true
solution($array, 6); // false

$fruits = ['apple', 'banana', 'orange'];
solution($fruits, 'banana'); // true
solution($fruits, 'grape'); // false
[You only need one - Beginner1.md](../../%D0%92%D0%B0%D0%B4%D0%B8%D0%BC/codewarsss/You%20only%20need%20one%20-%20Beginner1.md)